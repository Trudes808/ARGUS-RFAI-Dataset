# Intentionally invalid: two waveforms in slot 0 whose summed bandwidth + guard
# exceeds the 50 MHz block. The composer should raise a helpful error telling the
# user to split them into different blocks.
name: example_bad
output_dir: composites
zc_file: ../generated_sync_sequences/ZadoffChu_bw50MHz_R25_N601.mat
library_root: ../generated_waveforms_24576
blocks:
  - slots: [0]
    waveforms:
      - {mat: "5G_Downlink/5G_Downlink_bw50MHz_scs30kHz_pdsch16QAM_pn9.mat", power_db: 0}
      - {mat: "QPSK/QPSK_Rs15p36MHz_fosf4_occ18p432MHz_phase0_gray_rrc_a0p20_pn9.mat", power_db: 0}
